// Temp runner placeholder removed. This file previously contained a bare
// `Deno` identifier which causes `ts-node` or Node-based runners to throw
// when executed. Keep this file empty (or remove it) when running Node.

export {};